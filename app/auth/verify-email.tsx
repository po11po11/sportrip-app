import { StubScreen } from '@/components/common/stub-screen'

export default function VerifyEmailScreen() {
  return <StubScreen title="驗證信箱" description="Supabase email confirmation 流程預留，這版先以註冊成功後直接進入 app 為主。" />
}
