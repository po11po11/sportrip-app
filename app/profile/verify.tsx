import { StubScreen } from '@/components/common/stub-screen'

export default function ProfileVerifyScreen() {
  return <StubScreen title="實名認證" description="實名認證流程與證件上傳先保留為 TODO，不阻塞 Phase 1 demo。" />
}
